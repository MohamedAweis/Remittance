const userModel = require('./user.modal');
const permissionModel = require('./permission.modal');
const operationModel = require('./operation.model');
const remittanceModel = require('./remittance.model');
const customerModel = require('./customer.modal');


module.exports = {
    userModel,
    permissionModel,
    remittanceModel,
    operationModel,
    customerModel
};